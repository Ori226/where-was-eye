# Where Was Eye Configuration
# Copy this file to .env and update the values

# Required: Path to your Google Timeline JSON file
# Download from https://takeout.google.com/ (select Location History -> JSON)
LOCATION_HISTORY_PATH=/path/to/your/Location History.json

# Optional: OpenAI API Key for GPT integration
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Ollama configuration
# Defaults to localhost:11434
OLLAMA_HOST=http://localhost:11434

# Optional: Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Optional: CORS origins (comma-separated)
CORS_ORIGINS=*